import MainContent from "./MainContent";
import SideContent from "./SideContent";

const Content = () => {
  return (
    <main>
      <MainContent />
      <SideContent />
    </main>
  );
};

export default Content;
